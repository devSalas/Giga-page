---
import { dataBenefits } from "../../data"
import CardBenefit from "../CardBenefit.astro"

---

<div class="p-5 py-16 mb-16 md:mb-32">
    
    <h2 class="text-3xl  sm:text-4xl text-center font-extrabold mb-8 md:mb-16">
        <span class="text-cfourth"> ¿POR QUÉ ELEGIR NUESTRA </span><br>
        <span class="text-csecondary">FIBRA ÓPTICA?  </span>  
    </h2>
    <!-- <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8"> -->
    <div class="flex  gap-4 flex-wrap justify-center items-center ">
        {
            dataBenefits.map(item=> <CardBenefit img={item.img} title={item.title} text={item.text} />)
        }
    </div>

</div>