---
deployment:
  tasks:
    - echo "Starting Deployment"
    - export DEPLOYPATH=/home/gigamasc/public_html/repositories/Giga-page/dist/
    - /bin/cp -R * $DEPLOYPATH/
    - cd $DEPLOYPATH
    - mv dist/* .
    - echo "Deployment Finished"
